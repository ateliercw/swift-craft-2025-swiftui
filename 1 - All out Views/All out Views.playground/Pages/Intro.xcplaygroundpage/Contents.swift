/*: # Introduction

 Our journey into `SwiftUI` starts with exploring the constructs that make `Views` composition possible, with respect to the type system and generics, and how they allow `Views` to be more expressive.
 
 Reusability is one of the philosophies of `SwiftUI` and declarative programming in general. With this in mind, how can we better reason about structuring `Views`? Where do `ViewModifiers` come into play?
 */

//: [Next](@next)
